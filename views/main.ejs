<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose Your Role</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            overflow: hidden; /* Mencegah scroll pada halaman */
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            width: 100vw; /* Lebar dan tinggi container 100% dari viewport */
            flex-direction: column;
        }
        .box {
            width: 80%; /* Lebar kotak 80% dari container */
            height: 40vh; /* Tinggi kotak 40% dari viewport */
            background-color: #f0f0f0;
            border: 2px solid #333;
            margin: 20px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            display: flex;
            justify-content: center;
            align-items: center; /* Menempatkan tulisan di tengah kotak */
            text-decoration: none; /* Menghapus garis bawah default pada tautan */
            color: #333; /* Warna teks */
        }
        .box:hover {
            background-color: #ccc;
        }
        h1 {
            margin: 0; /* Menghapus margin default untuk h1 */
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="/pitcher" class="box">
            <h1>Pitcher</h1>
        </a>
        <a href="/investor" class="box">
            <h1>Investor</h1>
        </a>
    </div>
</body>
</html>
